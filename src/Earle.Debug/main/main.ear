cprint(inp) 
    print(inp);

entry ()
{
    sum = \other::count(23,4);
    cprint("Hello " + "World! (" + sum + ")");

    while(sum < 50)
    {
        sum++;
        cprint("SUM: " + sum);
    }

    for( i = 0 ; i <= 10 ; i = i + 1 )
    {
        cprint("FOR " + i);
    }
}