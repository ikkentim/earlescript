init()
{
	test = 1;
	print("test: " + test);
	test -= 5;
	print("test: " + test);

	thing = bleep;

	what = "bleep!";

	//what thing();

	thread what thing();

	for(i=0;i<10;i++)
	{
		print("wait..." + (1 + i));
		wait 1;
	}
	res = "this is the result";

	return res;
}

bleep()
{
	for(i=0;i<100;i++)
	{
		print(self);
		wait .1;
	}
}